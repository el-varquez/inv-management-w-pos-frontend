# Loaded automatically by Vite in development mode (`npm run dev`).
# Base URL of the backend API. Overrides the fallback in src/services/api.ts.
VITE_API_URL=https://inv-management-w-pos-backend.onrender.com/api
